{
  "devDependencies": {
    "sass": "^1.26.9"
  },
  "scripts": {
    "build": "rm -rf dist && parcel build src/index.html src/test.html --no-minify --public-url ./"
  },
  "dependencies": {
    "scss-lint": "0.0.0"
  }
}